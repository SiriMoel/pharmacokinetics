if (pharma_datura_effect_amount.x > 0.02) {

}

color.r -= (0.2 * pharma_datura_effect_amount.x);
color.g -= (0.4 * pharma_datura_effect_amount.x);
color.b -= (0.2 * pharma_datura_effect_amount.x);

color = color;